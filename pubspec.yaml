name: fdezplay
description: "FdezPlay IPTV"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.5
  cached_network_image: ^3.4.1
  flutter_secure_storage: ^10.3.1
  flutter_vlc_player: 7.4.3
  wakelock_plus: ^1.7.0
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependency_overrides:
  flutter_vlc_player_platform_interface: 2.0.4

flutter:
  uses-material-design: true
